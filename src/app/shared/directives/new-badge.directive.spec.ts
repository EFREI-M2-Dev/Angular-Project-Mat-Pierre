import { NewBadgeDirective } from './new-badge.directive';

describe('NewBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new NewBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
